import { SuggestedSearchDirective } from './suggested-search.directive';

describe('SuggestedSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new SuggestedSearchDirective();
    expect(directive).toBeTruthy();
  });
});
